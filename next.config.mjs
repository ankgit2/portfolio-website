/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
};

// Use "export default" instead of "module.exports"
export default nextConfig;